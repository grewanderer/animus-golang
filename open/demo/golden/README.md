# Golden demo transcripts

These files capture successful demo runs. Regeneration requires Docker daemon access.

Notes:
- The transcript files are only finalized on success.
- No-docker smoke transcripts require running services and DEMO_BASE_URL.
